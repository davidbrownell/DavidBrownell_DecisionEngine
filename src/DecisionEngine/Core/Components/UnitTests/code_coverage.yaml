# These includes and excludes will apply to all tests in the directory
# this file is placed in and its descendants.
filter:
  includes:
    - DecisionEngine::Core::Components::*
  continue_processing: false
